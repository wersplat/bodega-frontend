// types/match.ts

export interface RecentMatch {
  id: string;
  opponent: string;
  date: string;
  pts: number;
  ast: number;
  reb: number;
  stl: number;
  blk: number;
  // Add any additional fields as needed based on your API
}
