// Logger utility for the frontend

export function logError(message, _error) {
  // console.error(`[Error]: ${message}`, _error);
}

export function logInfo(message) {
  console.info(`[Info]: ${message}`);
}

export function logWarning(_message) {
  // console.warn(`[Warning]: ${_message}`);
}
